{
  "name": "proxy",
  "version": "0.0.0",
  "description": "proxy server",
  "main": "proxyserver.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
	"http-proxy": "^1.9.0"
  }
}
